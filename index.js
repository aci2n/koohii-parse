const fs = require("fs");
const program = require("commander");

function initProgram() {
    
}

function main() {
    initProgram();

    try {

    } catch(error) {
        console.error(error);
    }
}

main();